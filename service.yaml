apiVersion: v1
kind: Service

metadata:
  name: my-service
  namespace: dev

spec:
  type: NodePort

  selector:
    app: my-app

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007
